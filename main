from random import randint
N = int(input('Введите количество'))
mass = []
i = 0
while N > 0:
 i = randint(0,10)
 mass.append(i)
 N -=1
print(mass)

for k in range (0, N):
   mass[k] = mass[k] - mass[k-1]
print(mass)
